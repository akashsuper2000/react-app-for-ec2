{
  "name": "ec2-app",
  "version": "1.0.0",
  "scripts": {
    "client": "cd app-for-ec2 && yarn start",
    "server": "nodemon server.js",
    "start": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "express-validation": "^1.0.2",
    "mysql": "^2.17.1"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
